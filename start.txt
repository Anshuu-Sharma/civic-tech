To Run

  # Terminal 1 - API
  cd apps/api && npx tsx src/index.ts

  # Terminal 2 - Frontend
  cd apps/web && npx next dev

  # Terminal 3 - Voice Agent
  cd apps/voice-agent && uv run python agent.py dev